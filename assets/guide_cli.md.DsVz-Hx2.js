import{_ as t,H as d,f as o,i as a}from"./chunks/framework.BDVBA_aT.js";const u=JSON.parse('{"title":"CLI","description":"","frontmatter":{},"headers":[],"relativePath":"guide/cli.md","filePath":"guide/cli.md"}'),s={name:"guide/cli.md"};function i(c,e,n,r,l,p){return d(),o("div",null,[...e[0]||(e[0]=[a(`<h1 id="cli" tabindex="-1">CLI <a class="header-anchor" href="#cli" aria-label="Permalink to &quot;CLI&quot;">​</a></h1><p>The following commands are available in the terminal:</p><ul><li><code>typeorm-extension db create</code> to create the database</li><li><code>typeorm-extension db drop</code> to drop the database</li><li><code>typeorm-extension db drift</code> to assert that the database schema matches the entity metadata</li><li><code>typeorm-extension seed run</code> to seed the database</li><li><code>typeorm-extension seed create</code> to create a new seeder</li></ul><p>The legacy colon-form (<code>db:create</code>, <code>db:drop</code>, <code>seed:run</code>, <code>seed:create</code>) is still accepted as a backwards-compatibility alias for each command.</p><p>If the application has not yet been built and you want to run the CLI against TypeScript sources directly, invoke the ESM bundle with a TypeScript-aware Node loader (e.g. <code>tsx</code> or Node&#39;s <code>--experimental-strip-types</code>):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&quot;scripts&quot;: {</span></span>
<span class="line"><span>    &quot;db:create&quot;:  &quot;tsx ./node_modules/typeorm-extension/bin/cli.mjs db create&quot;,</span></span>
<span class="line"><span>    &quot;db:drop&quot;:    &quot;tsx ./node_modules/typeorm-extension/bin/cli.mjs db drop&quot;,</span></span>
<span class="line"><span>    &quot;db:drift&quot;:   &quot;tsx ./node_modules/typeorm-extension/bin/cli.mjs db drift&quot;,</span></span>
<span class="line"><span>    &quot;seed:run&quot;:   &quot;tsx ./node_modules/typeorm-extension/bin/cli.mjs seed run&quot;,</span></span>
<span class="line"><span>    &quot;seed:create&quot;:&quot;tsx ./node_modules/typeorm-extension/bin/cli.mjs seed create&quot;</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>Read the <a href="./seeding.html#configuration">Seeding Configuration</a> section to find out how to specify the path, for the seeder- &amp; factory-location.</p><h4 id="cli-options" tabindex="-1">CLI Options <a class="header-anchor" href="#cli-options" aria-label="Permalink to &quot;CLI Options&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Option</th><th>Commands</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>--root</code> or <code>-r</code></td><td><code>db create</code>, <code>db drift</code>, <code>db drop</code>, <code>seed create</code>, <code>seed run</code></td><td><code>process.cwd()</code></td><td>Root directory of the project.</td></tr><tr><td><code>--tsconfig</code> or <code>-tc</code></td><td><code>db create</code>, <code>db drift</code>, <code>db drop</code>, <code>seed run</code></td><td><code>tsconfig.json</code></td><td>Name (or relative path incl. name) of the tsconfig file.</td></tr><tr><td><code>--dataSource</code> or <code>-d</code></td><td><code>db create</code>, <code>db drift</code>, <code>db drop</code>, <code>seed run</code></td><td><code>data-source</code></td><td>Name (or relative path incl. name) of the data-source file.</td></tr><tr><td><code>--synchronize</code> or <code>-s</code></td><td><code>db create</code></td><td><code>yes</code></td><td>Synchronize the database schema after database creation. Options: <code>yes</code> or <code>no</code>.</td></tr><tr><td><code>--initialDatabase</code></td><td><code>db create</code></td><td><code>undefined</code></td><td>Specify the initial database to connect to. This option is only relevant for the <code>postgres</code> driver, which must always to connect to a database. If no database is provided, the database name will be equal to the connection user name.</td></tr><tr><td><code>--skipWithoutMigrations</code></td><td><code>db drift</code></td><td><code>false</code></td><td>Report no drift if the data-source has no migrations registered.</td></tr><tr><td><code>--name</code> or <code>-n</code></td><td><code>seed create</code> (required), <code>seed run</code> (optional)</td><td><code>undefined</code></td><td>Name (or relative path incl. name) of the seeder.</td></tr><tr><td><code>--timestamp</code> or <code>-t</code></td><td><code>seed create</code></td><td><code>Date.now()</code></td><td>Custom timestamp used in the generated seeder filename.</td></tr><tr><td><code>--javascript</code> or <code>-j</code></td><td><code>seed create</code></td><td><code>false</code></td><td>Generate a seeder file for JavaScript instead of TypeScript.</td></tr><tr><td><code>--preserveFilePaths</code></td><td><code>db create</code>, <code>db drift</code>, <code>db drop</code>, <code>seed run</code></td><td><code>false</code></td><td>This option indicates if file paths should be preserved and treated as if the just-in-time compilation environment is detected.</td></tr><tr><td><code>--log-level</code></td><td><code>db create</code>, <code>db drift</code>, <code>db drop</code>, <code>seed create</code>, <code>seed run</code></td><td><code>info</code></td><td>Logger verbosity. One of <code>silent</code>, <code>info</code>, <code>debug</code>.</td></tr></tbody></table><h2 id="schema-drift" tabindex="-1">Schema Drift <a class="header-anchor" href="#schema-drift" aria-label="Permalink to &quot;Schema Drift&quot;">​</a></h2><p><code>db drift</code> compares the database schema against the entity metadata and exits with code <code>1</code> if the two deviate, printing the statements which would reconcile them.</p><p>It is meant as a CI gate right after the migrations have run. A project which builds its schema with migrations in production but with <code>synchronize()</code> in tests has no guard against the two descriptions drifting apart, and the failure mode stays silent until someone generates the next migration, which may then contain data-destroying statements that look routine in review:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>migration run  -&gt;  revert x N  -&gt;  run  -&gt;  db drift</span></span></code></pre></div><div class="language-shell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tsx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./node_modules/typeorm-extension/bin/cli.mjs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> db</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> drift</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> src/data-source.ts</span></span></code></pre></div><p>Pass <code>--skipWithoutMigrations</code> to exit successfully if the data-source has no migrations registered. This is useful when the same data-source file serves a migration driven environment and a <code>synchronize()</code> driven one (e.g. <code>migrations: []</code> for an in-memory sqlite test database).</p><p>The check never writes to the database: the data-source built from the discovered options is built with <code>synchronize</code>, <code>migrationsRun</code> and <code>dropSchema</code> disabled.</p>`,16)])])}const m=t(s,[["render",i]]);export{u as __pageData,m as default};
